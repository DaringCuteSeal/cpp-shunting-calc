#include "result.hpp"

namespace result {

	/** Generic handler for a result.
	 * Usage:
	 * if (default_handler(&result, "parse tokens"))
	 * {
	 * 	// If process succeeded
	 * 	some_action;
	 * }
	 * else
	 * {
	 * 	// If process failed
	 * 	some_action;
	 * 	exit(1);
	 * }
	 * */
}
